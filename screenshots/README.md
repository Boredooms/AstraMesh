# Screenshots

Demo screenshots are added here as features land (see docs/IMPLEMENTATION.md §Screenshot Plan):

- nearby peers
- chat thread
- message delivered state
- file transfer state
- emergency broadcast state
- desktop companion dashboard
- website homepage

No screenshots are captured yet — that requires a physical device or emulator, which this
development environment doesn't have. The promotional website (`web/`) uses faithful HTML/CSS
recreations of the real Compose screens (same design tokens, same copy) in its Screenshots
section as an honest stand-in until real device captures are added here.
